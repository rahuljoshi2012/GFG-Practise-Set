class Solution
{
    public static void deleteKey(TrieNode root,char[] key)
    {
     TrieNode curr=root;
    for(char ch :key){
     curr=curr.subNode(ch);
       if(curr==null){
           return ;
       }
                
    }
    curr.isEnd =false;
          }
}
