class Solution
{
    public int peakElement(int[] arr,int n)
    {
       //add code here.
       int peek = arr[0];
       int pos = 0;
       for(int i=1;i<n;i++){
          if(arr[i]>peek){
              peek = arr[i];
              pos = i;
          } 
       }
       return pos;
    }
}
